# Notify

**Backlog:** T-079 · `notify`

Post a macOS notification from the CLI.

Not for: cross-platform notifications or Actionable Notification Center buttons.

## Usage

```bash
python3 notify.py 'Build done'
python3 notify.py -t 'Tests' -s Glass 'All green'
```
