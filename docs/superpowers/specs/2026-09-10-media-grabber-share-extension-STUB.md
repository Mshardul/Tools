# Share Extension (phase number TBD)

**Status:** stub — needs planning before any work.

> This file only reserves the epic. It is not a design. Do not implement
> from it. When the epic opens: brainstorm → full spec → plan → e2e Share
> Extension (no half-built appex).

**Depends on:** Phase 9 Home ingress / URL scheme so Share can hand off
without a second download pipeline.

**Phase number:** insert as Phase 10 when scheduled; renumber Diagnostics
and Polish in the same pass (parent §12.1 + backlog).

## Intent

User shares a URL from another app’s Share sheet → MediaGrabber appears in
the grid → main app opens and uses the same Home idle/busy path as Phase 9.

## Not this epic

- Phase 9 Services menu
- Chrome / Safari browser extensions (backlog; they should use the Phase 9
  URL scheme)

## When planning starts

Activation / App Group vs scheme-only handoff; share-grid display name;
signing; mockups; appex test strategy.
